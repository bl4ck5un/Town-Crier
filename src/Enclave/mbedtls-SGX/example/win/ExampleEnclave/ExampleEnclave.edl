enclave {
    from "..\..\lib\mbedtls_tlib.edl" import *; 

    trusted {
        /* define ECALLs here. */
        public int sgx_connect();
        public int sgx_accept();
    };

    untrusted {
        /* define OCALLs here. */
    
    };
};