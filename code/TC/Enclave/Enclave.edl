/* Enclave.edl - Top EDL file. */

enclave {
    trusted {
        public int ecall_connect([in, string] const char* server, [in, string] const char* port);
        public int ecall_self_test();
        public int ecall_client([in, string] const char* server, [in, string] const char* port);
    };

    untrusted {
    };

    from "..\TLSlib\TLSlib.edl" import *; 

};
