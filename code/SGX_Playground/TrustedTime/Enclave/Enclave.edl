enclave {
    from "sgx_tae_service.edl" import *;

    trusted {
        /* define ECALLs here. */
        public void showtime(void);
        public void create_session(void);
    
    };

    untrusted {
        /* define OCALLs here. */
        void ocall_print_string([in, string] const char *str);
    };
};
